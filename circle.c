#include <stdio.h>
int main()
{
    int radius = 2;
    float pi = 3.14;
    int area = pi * radius * radius;
    printf("print the area of circle %d\n", area);
    int height = 3;
    printf("Volume of this cylinder is %f\n", pi * radius * radius * height);

    return 0;
}